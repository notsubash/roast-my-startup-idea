services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      RUNS_DB_PATH: /data/runs.db
    volumes:
      - roast-data:/data
    restart: unless-stopped

volumes:
  roast-data:
